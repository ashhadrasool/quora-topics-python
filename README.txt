

Use this command to run the script
This would guarentee the working of script
pip install -r requirements.txt

for latest packages, you can run these commands:
pip install selenium
pip install openai
pip install python-wordpress-xmlrpc

download browser and driver from:
browsers downloaded from https://googlechromelabs.github.io/chrome-for-testing/
and paste in script containing folder like:
/lib/chromedriver-win64/
/lib/chrome-win64/

how to run from terminal/cmd
 python script.py --keyword business --n 3 --size medium --prompt "how to run"
